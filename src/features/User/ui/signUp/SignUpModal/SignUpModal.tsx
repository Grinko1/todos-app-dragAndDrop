import { memo } from 'react';
import cls from './SignUpModal.module.scss';

interface SignUpModalProps {
}

export const SignUpModal = memo((props: SignUpModalProps) => {
    const { } = props
    return (
        <div className={cls.SignUpModal}>
        </div>
    );
});